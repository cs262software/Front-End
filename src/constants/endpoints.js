var endpoints = {
    // LoginPage endpoints
    POST_LOGIN: "/api/login",
    POST_NEW_USER: "/api/login/create"

    // MainHeader endpoints.

    // HomePage endpoints.
};

export default endpoints;
